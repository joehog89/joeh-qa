# joeh-qa
Summary:
1. Set up the SSH key on our local mechine.
2. On github we added the .pub ssh key
3. We created a new repo on github
4. we cloned the repo via the ssh url (not https).

commands to learn:

- CLI:
cd
ls
mkdir
pwd
cat

- Git:
get clone <url>

git add <file>
git commit -m "your message here"
git push
